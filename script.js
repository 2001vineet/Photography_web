$('.carousel').carousel({
    interval: 3000, // Adjust the interval duration as needed (in milliseconds)
    pause: 'hover', // Pause on hover
    wrap: true // Wrap around to the first slide after reaching the last slide
  });


  